Creating a video from images involves combining multiple image frames, each captured at a specific moment in time, into a single video file. 
successive frames are almost identical because the scene isn't changing much. Redundant frames in a video are nearly identical or very similar consecutive frames that don't
add new visual information, often appearing as frozen moments or very slight shifts, and their removal (temporal redundancy reduction) is key for video compression and editing,
reducing file size and processing load.
